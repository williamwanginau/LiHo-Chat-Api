# Backend (NestJS)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app?schema=public
JWT_SECRET=please-change-this-to-a-strong-secret
PORT=4000
NODE_ENV=development

# JWT expiry (seconds). Defaults to 900 (15m) if unset
# JWT_EXPIRES_SEC=900

# Frontend env sample lives in web/.env.local.example

# CORS: comma-separated origins to allow.
# Dev/test default: allows localhost/127.0.0.1 when unset.
# Production default: blocks all cross-origin when unset. Set explicitly, e.g.:
# https://yourapp.example.com,https://www.example.com
# CORS_ORIGINS=

# Seed (safety): allow seeding in production only if explicitly set
# SEED_ALLOW_PROD=true

# Bcrypt hashing cost (salt rounds). Default is 10 if unset.
# BCRYPT_ROUNDS=10

# Prisma connect on boot. Set to false for tests/offline.
# PRISMA_CONNECT_ON_BOOT=true
